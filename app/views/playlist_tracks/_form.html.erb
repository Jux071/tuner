<%= form_for(@playlist_track) do |f| %>
  <% if @playlist_track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playlist_track.errors.count, "error") %> prohibited this apprenticeship from being saved:</h2>

      <ul>
      <% @playlist_track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :playlist_id, Playlist.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div class="field">
    <%= f.select :track_id, Track.all.collect { |p| [ p.name, p.id ] } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>