<% provide(:title, "New Artist") %>
<div class = "well well-sm">
	<h1 style = "text-align: center">Add new artist</h1>
	<%= render 'shared/error_messages', model: @artist %>
	<%= render 'form' %>
</div>
<%= render 'back' %>