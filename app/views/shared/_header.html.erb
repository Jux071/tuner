<% if logged_in? %>
<nav class="navbar navbar-default">
	<ul class="nav navbar-nav">  
	<a class="navbar-brand">TUNER</a>    
    <li><%= link_to "Profile", current_user %></li>
          <li><%= link_to "Settings", '#' %></li>
          <% if current_user.admin? %>
            <li><%= link_to "Dashboard", admin_dashboard_path %> </li>
          <% end %>
<form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
       <li><%= link_to session_path, method: :delete do %>
            <i span class="glyphicon glyphicon-log-out"></i> Logout
          <% end %>
          </li>

          
          <p class="navbar-text">Signed in as: <a><i span class="glyphicon glyphicon-user"></i> <%= current_user.username %></a></p> 
    





          

        </ul>
      </li>
<% end %>
 </ul>
 </nav>