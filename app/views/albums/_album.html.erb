<tr>
  <td><%= link_to album.artist.name, artist_path(album.artist_id) %></td>
	<td><%= link_to album.title, album_path(album) %></td>
	<td><%= album.released %></td>
	<td><%= link_to album.genre.name, genre_path(album.genre_id) %></td>		
</tr>