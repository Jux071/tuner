<% Album.all.order('albums.created_at DESC').limit(5).each do |album| %>
	<table class="table table-hover table-striped">	
		<tbody>
			<tr>
			  <td><%= album.artist.name %> - <%= album.title %></td>
			</tr>
		</tbody>
	</table>
<% end %>