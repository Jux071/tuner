<% provide(:title, "New Playlist") %>
<div class = "well">
	<h1 style = "text-align: center">Add new playlist</h1>
	<%= render 'shared/error_messages', model: @playlist %>
	<%= render 'form' %>
</div>
<%= render 'back' %>